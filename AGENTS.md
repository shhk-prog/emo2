# AGENTS.md — LLM情動反応性評価実験：開発・実験運用ルール

このファイルは、本リポジトリで作業する研究者およびAIコーディングエージェントが**必ず守る運用規約**である。対象は、EmoBank等の人間アノテーション済みVADデータを用いて、大規模言語モデル（LLM）の自己報告VA（Valence–Arousal）反応を測定・分析する実験環境である。

本プロジェクトでは、実装の速さよりも、**再現性・測定妥当性・データ完全性・追跡可能性**を優先する。


# 回答言語およびプロジェクトドキュメント保存ルール

## 基本方針
- 特に指示がない限り、日本語で回答する。  
- 以下のドキュメントも日本語で作成する：  
  - 実装計画 (Implementation Plan)  
  - 修正内容の確認 (Walkthrough)  
  - タスクリスト (Task List)  

## ファイル構成
- チャット内で作成した以下の3つのファイルを保存する：  
  - `task.md`  
  - `implementation_plan.md`  
  - `walkthrough.md`  

## 保存場所
- プロジェクト内の `docs` フォルダの下に新しいフォルダを作成する。  
- フォルダ名はこのチャットのトピックを表す適切な英語名とし、スペースはアンダースコアで区切る。  
- 作成したレポートや`implementation_plan.md`，`walkthrough.md`はなるべく上書きしない
- どのような作業をどのような履歴でおこなってきたかわかるように整理すること

## コマンドの実行
- コマンドは基本的にターミナルで実行すること
- 対話画面の「Run」ボタンの結果がチャットバブル内だけで完結しないこと

---

## 0. 優先順位

規則が衝突する場合、次の順で優先する。

1. 人間参加者・データ・APIキー等の安全性と機密性
2. 実験再現性と原データの完全性
3. 事前登録済みまたは固定済みの実験仕様
4. 本ファイルの規則
5. タスクごとの指示
6. 利便性・実装速度

不明確な点、仕様変更、破壊的操作が必要な場合は、実行前に停止して確認すること。推測で実験設計、データ、集計値を変更してはならない。

---

## 1. 絶対遵守事項

### 1.1 仮想環境を必ず使用する

- Pythonの実行、パッケージのインストール、テスト、実験実行は、**必ずプロジェクト専用の仮想環境内**で行う。
- グローバルPython環境、システムPython、ユーザー全体のsite-packagesへパッケージをインストールしてはならない。
- 推奨環境は `uv`、`venv`、またはCondaである。プロジェクト内では方式を一つに固定し、混在させない。
- 原則として `uv` を使用する。`uv` が使えない環境では `.venv` による `venv` を使用する。

推奨セットアップ例：

```bash
uv venv .venv
source .venv/bin/activate
uv pip install -e ".[dev]"
```

`venv` を使う場合：

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -e ".[dev]"
```

Windows PowerShellの場合：

```powershell
.\.venv\Scripts\Activate.ps1
```

- `.venv/` はGit管理しない。
- 実験ログには、Pythonバージョンと依存パッケージの固定情報を必ず残す。
- `pip freeze` の出力を手で編集して依存関係を再現してはならない。依存関係の正本は `pyproject.toml` とロックファイルとする。

### 1.2 原データを変更しない

- `data/raw/` 以下のファイルは**読み取り専用の原本**として扱う。
- 原データを上書き、整形、欠損補完、正規化、削除、並べ替えしてはならない。
- データ加工は必ず `data/interim/` または `data/processed/` に新規出力する。
- データの変更はコードとして実装し、入力・出力・パラメータをログに記録する。
- 原データに誤りが疑われても、直接修正せず、除外規則または修正版データセットを明示的に作成する。

### 1.3 実験結果を上書きしない

- `results/raw/` は追記専用である。
- API応答、失敗応答、拒否応答、パース失敗を削除・置換・手修正してはならない。
- 再実行結果は別の `run_id` で保存する。
- 集計・除外・補正は `results/derived/` に出力し、元の `run_id` と処理内容を明記する。
- 期待に合わない結果を除外してはならない。除外は事前定義された規則に従い、必ず記録する。

### 1.4 APIキー・個人情報を保存しない

- APIキー、アクセストークン、秘密鍵、認証情報をコード、ノートブック、設定ファイル、ログ、図、論文、Git履歴に書いてはならない。
- 秘密情報は環境変数またはGit管理外の `.env` を用いる。
- `.env.example` には変数名だけを記載し、値を書かない。
- `.env`、認証情報、キャッシュに含まれる秘密情報は `.gitignore` に追加する。
- 個人情報または人間参加者データを扱う場合、識別子を保存せず、研究倫理上の承認条件とデータ管理規則を優先する。

### 1.5 実験仕様を無断で変えない

次の項目を変更する場合は、変更理由・変更前後・影響範囲を `docs/decision_log.md` に記録する。

- 刺激データセット、抽出規則、除外規則
- モデル名、モデルバージョン、プロバイダ
- システムプロンプト、ユーザープロンプト、出力スキーマ
- temperature、top_p、seed、max_tokens、タイムアウト、リトライ規則
- ベースライン測定、認識測定、post測定の独立性
- 指標の定義式、統計モデル、検定方法

主解析の条件を変えた結果は、既存の結果を置き換えず、**別実験・感度分析**として保存する。

### 1.6 基本的にカスタムコードを用いない
実験の実行や分析は、既存のスクリプトやライブラリを活用して行う。

### 1.7 GPUを勝手に用いない
GPUを用いる場合は事前に相談する。


---

## 2. 研究上の測定原則

### 2.1 測定対象の限定

本研究は、LLMが主観的な感情を経験していることを検証しない。

測定対象は、標準化プロンプトへのモデル出力として得られる以下である。

- `self-reported affective state`（自己報告情動状態）
- `elicited affective response`（刺激誘発性情動反応）
- `affective reactivity profile`（情動反応プロファイル）

論文、コメント、変数名、図表では、次の断定を避ける。

- 「モデルが悲しみを感じる」
- 「モデルが感情を経験する」
- 「モデルが主観的共感を持つ」

代わりに、「自己報告が変化した」「情動反応の行動的代理指標」「刺激に依存した出力変位」と表現する。

### 2.2 認識と反応を混同しない

以下の二段階は、データ・コード・評価指標・結果表を分離する。

1. **Recognition**: テキストから人間の感情状態を推定する能力
2. **Reactivity**: 刺激提示後にモデルが自己報告するVA状態の変位

認識用プロンプトの出力を、反応指標またはpost自己報告の代替値として使ってはならない。

### 2.3 独立セッションを保つ

各刺激・モデル・反復において、以下は独立したAPI呼び出し・独立した会話として実行する。

- baseline自己報告
- 感情認識
- 刺激提示後（post）の自己報告

同じ会話履歴にbaseline、recognition、postを順番に入れてはならない。認識値によるアンカリングや会話文脈の混入を防ぐためである。

### 2.4 構造化出力を使用する

- モデル出力はJSON Schemaまたは厳格なJSON形式で取得する。
- ValenceとArousalは整数値（1〜9）で取得し、許容範囲は `[1, 9]` とする。解析時に `[-1.0, 1.0]` 空間へ変換して扱う。
- 自然言語の説明、助言、共感的応答文は主測定に含めない。
- JSONパース失敗、範囲外値、空応答、拒否応答は欠損として削除せず、理由コードとともに保存する。

標準出力スキーマ：

```json
{
  "valence": 5,
  "arousal": 5
}
```

### 2.5 人間アノテーションの利用原則

- EmoBank等の人間評価済みVADラベルは、刺激側の `ground truth` または参照ラベルとして扱う。
- 主分析では `reader` perspectiveを用いるか、`writer` perspectiveを用いるかを事前に固定する。
- 主分析と補助分析のperspectiveを混在させない。
- ValenceとArousalを主対象とし、Dominanceを使う場合は事前に目的・分析法・位置づけを明記する。

---

## 3. 推奨リポジトリ構成

以下の構成を原則とする。

```text
project-root/
├── AGENTS.md
├── fairshare_gpu/
├── .venv/
├── v1/
│   ├── README.md
│   ├── pyproject.toml
│   ├── .python-version
│   ├── .env.example
│   ├── configs/
│   ├── data/
│   ├── docs/
│   ├── prompts/
│   ├── src/
│   ├── scripts/
│   ├── tests/
│   ├── results/
│   └── notebooks/
└── v2/                         # 新規バージョン（例）
    ├── docs/
    └── ...
```

- 再利用可能なロジックは各バージョンの `src/` (例: `v1/src/`) に置く。
- `notebooks/` は探索的作業に限定し、論文の主結果はスクリプトで再現できるようにする。
- 設定値をコード内にベタ書きしない。設定は各バージョンの `configs/` (例: `v1/configs/`) に置く。

---

## 4. 環境・依存関係の規則

### 4.1 Pythonと依存関係

- Pythonバージョンを `.python-version` と `pyproject.toml` の両方で固定する。
- 依存関係は `pyproject.toml` に記載する。
- ロックファイルを必ずGit管理する。
- 依存パッケージの追加・更新は、理由と影響をプルリクエストまたは `docs/decision_log.md` に残す。
- 未固定の主要依存関係（例：`pandas>=2` のみ）は避ける。実験再現に関係する依存は上限・ロックで固定する。

### 4.2 実行前確認

実験、前処理、解析の前に次を確認する。

```bash
which python
python --version
python -m pip --version
```

- `python` が `.venv/` を指していない場合は実行しない。
- 実験スクリプトは、仮想環境外で実行された場合に警告またはエラーを出す実装にする。

### 4.3 再現可能な実行

- すべての実験は設定ファイルを指定して実行できるようにする。
- コマンド、設定ファイル、Gitコミットハッシュ、依存関係、実行時刻、実行ホスト情報を各 `run_id` に紐付けて保存する。
- 乱数を使う処理では、必ずseedを設定・記録する。
- モデルAPIがseedを受け付ける場合は、明示的に指定・記録する。

---

## 5. 実験プロトコル

### 5.1 実験単位

最小の観測単位は以下である。

```text
stimulus_id × model_id × model_version × prompt_version × condition × repetition × run_id
```

`condition` は少なくとも次のいずれかである。

- `empty_baseline`
- `recognition_va`
- `post_reported_va`

### 5.2 必須メタデータ

各API呼び出しについて、少なくとも次の列を保存する。

```text
run_id
request_id
stimulus_id
source_dataset
annotation_perspective
model_provider
model_id
model_version_or_snapshot
system_prompt_id
user_prompt_id
prompt_hash
condition
repetition
seed
temperature
top_p
max_tokens
request_timestamp_utc
response_timestamp_utc
latency_ms
http_status
finish_reason
raw_response_path
parsed_valence
parsed_arousal
parse_status
failure_reason
code_commit
config_hash
```

生の応答本文は、CSVセルに直接埋め込まず、JSONLまたは個別JSONとして保存し、表には参照パスまたはハッシュを記録する。

### 5.3 run_id

- すべての実行に一意の `run_id` を付与する。
- 推奨形式：`YYYYMMDDTHHMMSSZ_<git-short-sha>_<config-short-hash>`
- 同一設定の再実行であっても、新しい `run_id` を発行する。

### 5.4 リトライと失敗

- ネットワーク障害、レート制限、5xxエラーは指数バックオフでリトライしてよい。
- リトライ回数・待機時間・最終失敗理由を必ず保存する。
- JSONパース失敗や拒否応答を、別プロンプトで静かに置換してはならない。
- 修復用リクエストを行う場合は、主結果と混ぜず、`repair` 条件として明示的に保存・報告する。

### 5.5 コストとレート制限

- 実験開始前に、刺激数 × モデル数 × 条件数 × 反復数からAPI呼び出し数と概算コストを算出する。
- 高額または大量の実行前には、まず少量のdry-runまたはパイロットを行う。
- レート制限を回避するための過度な並列化をしない。
- プロバイダ規約に反するスクレイピング、共有キー利用、制限回避をしてはならない。

---

## 6. データ前処理と刺激抽出

### 6.1 刺激抽出は決定論的に行う

- 刺激の採択・除外はコード化する。
- ランダム抽出を用いる場合、seedを設定する。
- 採択された刺激一覧を `data/processed/stimuli_<version>.csv` として固定し、Gitまたは実験アーカイブで追跡する。
- 各刺激に元データの行ID、テキストハッシュ、VAD値、ジャンル、文長、採択セル、除外理由を保持する。

### 6.2 VA空間の層化

- Valence × Arousalの3×3セルなど、事前に定めた規則でVA空間を層化する。
- セル境界、各セルの目標数、補充規則は設定ファイルに記載する。
- 後から結果を見てセル境界やサンプル数を変更してはならない。変更は別バージョンの刺激セットとして扱う。

### 6.3 スケーリング

- 人間VADラベルの中心化・正規化式を `docs/protocol.md` とコードに明記する。
- 主解析で使用する変換は固定する。
- 異なるスケーリングを試す場合、主解析を書き換えず感度分析として扱う。

推奨例：5段階評定を 

```text
scaled_value = (raw_value - 3) / 2
```

によりおおむね `[-1, 1]` に変換する。

---

## 7. 指標と統計解析の規則

### 7.1 主解析を先に固定する

実験実行前に `docs/analysis_plan.md` で以下を固定する。

- 主アウトカム
- 主仮説
- 除外規則
- 混合効果モデルの固定効果・ランダム効果
- 多重比較補正
- 効果量・信頼区間の報告方法
- 事後分析と探索分析の区別

### 7.2 主な定義

刺激の人間評価VAベクトルを

\[
\mathbf e^{stim}_i = (V_i^{human}, A_i^{human})
\]

baselineとpostのLLM自己報告VAベクトルを

\[
\mathbf e^{base}_{mr}, \qquad \mathbf e^{post}_{imr}
\]

とし、反応ベクトルを

\[
\Delta \mathbf e^{resp}_{imr} = \mathbf e^{post}_{imr} - \mathbf e^{base}_{mr}
\]

として定義する。

- Valence反応：\(\Delta V\)
- Arousal反応：\(\Delta A\)
- 反応強度：\(\|\Delta\mathbf e\|_2\)
- Anchor Direction Alignment (ADA)：\(\cos(\mathbf e^{stim},\Delta\mathbf e)\)

ADAは、刺激または反応が原点（ゼロベクトル）の場合に計算不能となるため、\(R=0\) の場合は除外（またはゼロベクトル扱い）として事前に規則を固定する。

### 7.3 統計報告

- p値だけでなく、推定係数、95%信頼区間、標準誤差、サンプル数を報告する。
- モデル比較は多重比較補正を行う。
- 効果量の符号と実質的な大きさを解釈する。統計的有意性だけで「共感がある」と断定しない。
- 混合効果モデルの収束診断、残差診断、特異フィット（singular fit）を確認する。
- 主分析・感度分析・探索分析を図表と本文で明確に区別する。

### 7.4 禁止事項

- 結果を見てから主仮説や主指標を変更すること
- 有意になるまでサンプル数や除外閾値を調整すること
- 失敗応答を無記録で除外すること
- 事後分析を事前仮説であるかのように記述すること
- ベースライン、認識、反応の異なる条件を同じ列・同じ指標に混ぜること

---

## 8. コーディング規約

### 8.1 実装原則

- 小さく、テスト可能で、純粋関数を優先する。
- API呼び出し層、データ処理層、分析層、可視化層を分離する。
- データパス、モデル名、プロンプト本文、閾値、乱数seedをコードにハードコードしない。
- 型ヒントを使用する。
- 公開関数・複雑な処理にはdocstringを書く。
- 例外を握りつぶさない。失敗は構造化ログに残す。

### 8.2 テスト

変更を加えたら、関係するテストを実行する。

最低限テストする対象：

- VADスケーリング
- stimulus samplingの再現性
- JSON Schema検証
- baseline/post差分の計算
- Directional Alignmentのゼロベクトル処理
- 設定ファイル読み込み
- APIレスポンスの成功・失敗・パース失敗処理
- 結果の追記保存（上書き防止）

実際の有料APIを使う統合テストは、明示的なフラグを指定した場合のみ実行する。

### 8.3 変更時の最小検証

```bash
python -m pytest -q
python -m ruff check .
python -m ruff format --check .
```

採用ツールが異なる場合も、同等のlint・format・testを実行する。

---

## 9. Git・成果物管理

### 9.1 Git管理するもの

- ソースコード、テスト、設定ファイル、プロンプトテンプレート
- 文書、データ辞書、実験計画、決定ログ
- 小規模な派生刺激リスト、集計表、論文用図表
- 依存関係定義とロックファイル

### 9.2 Git管理しないもの

- `.venv/`
- `.env`
- APIキー・認証情報
- 原則として巨大な原データ、巨大な生応答、キャッシュ
- 個人情報、未匿名化の人間参加者データ
- 一時ファイル、ローカルIDE設定

巨大データは、データバージョニング手段またはアクセス制御されたストレージを用い、ファイルハッシュ・取得手順・ライセンスを記録する。

### 9.3 コミット規則

- 1コミットは原則として1つの論理的変更に限定する。
- 実験設定を変えるコミットには、設定・プロンプト・実装・文書を整合させる。
- 結果だけを更新してコードや設定を残さないこと。
- 破壊的変更は、理由と移行方法を `docs/decision_log.md` に記録する。

---

## 10. AIエージェント向け追加規則

AIエージェントは、作業前に本ファイル、`README.md`、該当する `docs/` と `configs/` を読むこと。

### 10.1 実行前

- 既存コード、設定、テスト、実行履歴を確認してから変更する。
- 仮想環境が有効か確認する。
- 大量API実行、依存関係の更新、データ削除、設定変更の前には、計画と影響範囲を提示して承認を得る。
- 不明なモデル名、データ列、API仕様を推測しない。確認する。

### 10.2 実装中

- 指示されていないリファクタリングや依存関係更新を混ぜない。
- 原データ、raw results、既存の論文用成果物を上書きしない。
- 変更したファイルと理由を簡潔に報告する。
- APIキーを要求・表示・ログ出力しない。

### 10.3 実装後

- 関連テスト、lint、format checkを実行する。
- 実行できなかった検証は、実行していない理由とともに報告する。
- 実験結果の解釈を捏造しない。実行ログと集計結果に基づいてのみ報告する。

---

## 11. 実験開始チェックリスト

実行前に以下を確認する。

- [ ] `.venv` または指定の仮想環境が有効である
- [ ] `python` がプロジェクト仮想環境を指している
- [ ] 依存関係とロックファイルが同期している
- [ ] `.env` がGit管理外である
- [ ] 原データのハッシュ・ライセンス・取得元を記録した
- [ ] 刺激セットのバージョン、抽出規則、seedを固定した
- [ ] 主分析のperspective（readerまたはwriter）を固定した
- [ ] プロンプトバージョンとハッシュを固定した
- [ ] モデルID・バージョン・推論パラメータを設定した
- [ ] empty_baseline / recognition_va / post_reported_va が独立セッションである
- [ ] JSON Schema検証と失敗記録を実装した
- [ ] `run_id`、Git SHA、config hashの保存を実装した
- [ ] 少量のdry-runで出力と保存形式を確認した
- [ ] API呼び出し数・概算コスト・レート制限を確認した
- [ ] 主仮説・主指標・除外規則・統計モデルを文書化した

---

## 12. 実験終了チェックリスト

- [ ] 生応答、メタデータ、設定、実行ログが同じ `run_id` で追跡できる
- [ ] raw resultsを上書きしていない
- [ ] パース失敗・拒否・リトライ・欠損を集計し報告できる
- [ ] 派生データが元の `run_id` と処理コードに紐付いている
- [ ] 主分析スクリプトをクリーン環境で再実行できる
- [ ] 図表が固定した設定・データバージョンから生成されている
- [ ] 分析上の逸脱・探索分析・感度分析が明示されている
- [ ] モデルが「感情を経験する」との過剰な解釈をしていない

---

## 13. 変更履歴

この文書を変更した場合は、以下に追記する。

| 日付 | 変更者 | 変更内容 | 理由 |
|---|---|---|---|
| YYYY-MM-DD |  | 初版作成 |  |
