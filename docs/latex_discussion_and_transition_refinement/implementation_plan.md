# 実装計画書: LaTeX構成案（iclr2027_conference2.tex）の考察・接続検証・重複修正

## 1. 目的とスコープ
`/mnt/nas/home/hiromi/src/emo2/iclr2027/iclr2027_conference2.tex` において、各ステージ（Behavioral, V1, V2, V3）の「結果」「考察」「次のステージへの接続」、および「全体の考察」を対象とし、以下の作業を行う。
1. **結果との整合性検証**: 考察・接続の記述内容が、提示された実測データ・検定統計量・Gate判定と正確に合致しているか確認・整合。
2. **無駄な重複の削減・整理**: 同一の免責文言や重複した論理展開、数式表記の冗長性をスリム化。
3. **空欄セクションの補完**:
   - `\section{V2結果}` のテーブル参照・骨子確認
   - `\section{V2考察}` の学術的記述（H1〜H4の結果と解釈、静的測定の限界）
   - `\section{V2からV3への接続}` の論理展開（静的幾何から動的時空間因果利用への移行）
   - `\section{全体の考察}` の総括（4段階証拠階層の統合、三重の境界線、AI安全性・擬人化への警鐘）

---

## 2. 精査結果と修正方針

### 2.1 Behavioral考察 & BehavioralからV1への接続
- **現状**: $r_\Delta = 0.56\text{--}0.93$、EmoBank対応のモデル依存性、感度・単調性・特異性の限界が的確に書かれている。
- **改善点**:
  - 「LLMが人間と同様の感情を持つことを意味しない」という趣旨の免責文言がLine 2906とLine 2931で重複しているため、末尾にすっきりと統合。
  - 接続セクションは「出力共変動 $\to$ 内部表現同一性」の3つの仮説（共通表現、形式先入観、一般的刺激特徴）が極めて論理的であるため、これを維持しつつ簡潔化。

### 2.2 V1考察 & V1からV2への接続
- **現状**: E1（デコード可能、深度近接）、E2（RSA正、直交負、Procrustes回復）、Phase B（Word Shuffle低下）、E3（層プロファイル正相関、方向差）、E4（介入特異性なし、Negative Result）、E6（部分特殊化）と完全に結果と一致。
- **改善点**:
  - 全角ピリオド「．」（Line 4522）の修正。
  - Line 4528-4541に存在する4連続の独立行立て数式（`\[ ... \]`）をインライン・数式配列に統合し、レイアウトの間延びを解消。

### 2.3 V2結果・V2考察・V2からV3への接続（★空欄の補完）
- **現状**: `V2考察`（Line 6105）および `V2からV3への接続`（Line 6113）が空欄。
- **修正内容**:
  - **V2考察の執筆**:
    - **H1a（幾何再編）**: 事後学習に伴い有意なProcrustes Distortion（Reader平均~0.500, Self平均~0.537）が観測され、内部情動幾何が再編される。
    - **H1b（デコード深度変位）**: 一様な深層シフト（$\Delta d^* > 0$）は支持されず（paired diff $p=1.0$）、ファミリー依存。
    - **H2（表現共有度変化）**: Matched-Plain条件ではBaseからInstructへの変化に伴いReader--Self表現共有度 $\Delta\text{Sharing}$ が有意に低下する傾向（平均 $-0.553$ [$-1.324, -0.066$]）が見られた一方、Native Chat条件ではプロンプトフォーマット差異による大きな見かけの変動（Format Effect）が確認され、プロンプト統制の重要性が示された。
    - **H3（因果プロファイルLMM）**: `c_net_rand` によるLMMにおいて、事後学習と層深度の交互作用項はFDR補正後に有意水準に達せず、単純な深層への因果leverage移行は普遍的法則とは言えない。
    - **H4（分布回復）**: 4ファミリー完全評価では不完全・単一特異モデルの限界があり、事後学習モデルの自己報告分布をBaseモデルの内部表現から普遍的に再構成できるとは結論できない。
    - **静的測定の限界**: プロンプト末尾（prompt-end）での静的測定では、トークン生成が動的に進行する過程（いつ因果利用されるか）を捉えられない。
  - **V2からV3への接続の執筆**:
    - 静的・層横断的な比較（Base vs Instruct）から、Instructモデル内部の動的な生成計算過程（Spatiotemporal Causal Dynamics）への移行。
    - 「Decodable $\neq$ Causal Leverage」の分離。
    - 事前Gate（State Induction Gate）による操作可能性の担保を経て、生成時間軸（temporal stage）と空間軸（layer depth）の双方にわたる時空間因果利用マップを検証するV3への展開。

### 2.4 V3考察
- **現状**: 事前Gate NO_GO、Qwen Discoveryでの解離（$\Delta d^* < 0$）、ConfirmatoryでのH1〜H3不成立、H4 Temporal Contrastの有意な再現シグナル、All Confirmed = NOという厳格な科学的境界が完璧に書かれている。
- **改善点**: 記述内容を維持し、次セクション「全体の考察」への論理の引き渡しを整流化。

### 2.5 全体の考察（★空欄の補完）
- **現状**: `全体の考察`（Line 8085）が空欄。
- **修正内容**:
  - **4段階の証拠階層の統合的総括**:
    1. Behavioral（出力共変動） $\to$ 2. V1（内部表現の部分共有・因果非互換性） $\to$ 3. V2（事後学習による幾何再編とフォーマット効果） $\to$ 4. V3（局所的・動的感度シグナルと普遍的因果メカニズムの非支持）。
  - **「情動デコード可能性 $\neq$ 因果的利用 $\neq$ 主観的経験」の三重の境界線**:
    - 線形プローブで高精度にデコードできることと、モデルがそれを生成に因果利用していること、そしてモデルが主観的感情を体験していることの間の論理的飛躍を厳格に退ける。
  - **AI安全性と臨床・対話システムへの示唆**:
    - 表層的な感情自己報告に基づく擬人化や過剰信頼の危険性。
    - 情動ステアリング技術の限界と、生成時間軸を考慮した安全性評価の必要性。

---

## 3. 検証手順
1. `iclr2027_conference2.tex` への変更適用。
2. 変更前後の差分と論理の整合性を入念に精査。
3. LaTeXの構文エラー（開き括弧・閉じ括弧、未定義参照、環境の整合）がないことを確認。
4. `docs/latex_discussion_and_transition_refinement/walkthrough.md` に結果を詳細に記録。
