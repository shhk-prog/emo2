# Walkthrough: README の詳細化

コードは変えていない。現行実装と YAML に合わせて本文を厚くした。

- ルート: 統一枠組み、データ対応表、production bash と統合 CLI の差、結果管理、論文正本
- Behavioral: AIPsy 4-split の意味、V3 とのデータの分け方、出力列
- V1: Phase ごとの成果物、E6 No-Go
- V2: YAML に沿った共通設計（形式統制、held-out、recovery、dry-run fixture）
- V3: ローダー、方向の Primary/Secondary、joint patch、ゲートファイル、`n_causal_samples: 15`
- 各 `primary/README.md`: 実行順、引数、禁止事項

旧論文の数値は書いていない。
